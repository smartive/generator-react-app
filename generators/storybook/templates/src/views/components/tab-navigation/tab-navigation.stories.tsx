import { storiesOf } from '@storybook/react';
import React from 'react';

import { TabNavigation } from '.';

storiesOf('03 — Components / Tab Navigation', module).add('Basic', () => <TabNavigation />);
